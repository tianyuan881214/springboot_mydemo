<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.springboot_mybatis_oracle.dao.PersonDao">
    <select id="findById" resultType="com.example.springboot_mybatis_oracle.model.Person">
        SELECT * FROM PERSON WHERE ID=#{id}
    </select>
    <select id="findAll" resultType="com.example.springboot_mybatis_oracle.model.Person">
        SELECT * FROM PERSON
    </select>
</mapper>